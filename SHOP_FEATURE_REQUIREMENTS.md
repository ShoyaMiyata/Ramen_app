# 店舗詳細機能 - 要件定義書

## 概要
ラーメン店舗単位で投稿を閲覧できる機能を実装する。ユーザーは店舗名で検索し、店舗詳細ページでその店舗に関する全ての投稿（写真・レビュー）を一覧表示できる。

## 現状分析

### 既存のデータ構造
- **shops テーブル**: 店舗情報（name, address, prefecture, station）
- **noodles テーブル**: ラーメン投稿（userId, shopId, ramenName, genres, evaluation, imageIds等）
- **既存API**: `shops.search` (店舗名検索)

### 既存機能
✅ 店舗名での検索機能（`shops.search`）
✅ ユーザー単位での投稿一覧表示（`noodles.getByUser`）
❌ 店舗単位での投稿一覧表示（未実装）
❌ 店舗詳細ページ（未実装）

## 機能要件

### 1. 店舗検索画面（新規または既存拡張）

#### 1-1. 検索機能
- **入力**: 店舗名（部分一致検索）
- **表示項目**:
  - 店舗名
  - 住所（ある場合）
  - 都道府県
  - 最寄り駅（ある場合）
  - 投稿数（この店舗への総投稿数）
  - 平均評価（星の数）

#### 1-2. 検索結果の並び順
- デフォルト: 投稿数の多い順
- オプション:
  - 評価の高い順
  - 50音順

#### 1-3. UI/UX
- リアルタイム検索（入力中に候補表示）
- 検索結果は10件まで表示
- 各店舗カードをタップ → 店舗詳細ページへ遷移

### 2. 店舗詳細ページ（新規作成）

#### 2-1. ページURL
`/shops/[shopId]`

#### 2-2. 表示情報

**ヘッダーセクション**:
- 店舗名（大きく表示）
- 住所
- 都道府県
- 最寄り駅
- 統計情報:
  - 総投稿数
  - 訪問ユーザー数（ユニークユーザー数）
  - 平均評価（星表示）
  - 評価分布（5つ星〜1つ星の件数）

**投稿一覧セクション**:
- この店舗への全投稿を時系列で表示（新しい順）
- 各投稿の表示項目:
  - 投稿者のアイコン・名前
  - 投稿日
  - メニュー名
  - 評価（星）
  - 写真（複数枚対応）
  - コメント
  - ジャンルタグ
  - いいね数

**フィルター機能**:
- 評価でフィルタ（5つ星のみ、4つ星以上等）
- ジャンルでフィルタ
- 期間でフィルタ（直近1ヶ月、3ヶ月、1年等）

**ソート機能**:
- 新しい順（デフォルト）
- 評価の高い順
- いいね数の多い順

#### 2-3. UI/UX
- 無限スクロールまたはページネーション
- 写真タップ → 拡大表示（ギャラリーモード）
- 投稿をタップ → 投稿詳細ページへ遷移
- 投稿者名タップ → ユーザープロフィールへ遷移
- 戻るボタン → 検索画面または前のページへ

### 3. アクセス導線

#### 3-1. メイン導線
1. ホーム画面 → 「店舗を探す」ボタン → 店舗検索画面
2. 店舗検索画面 → 店舗名入力 → 検索結果表示
3. 検索結果から店舗選択 → 店舗詳細ページ

#### 3-2. サブ導線
- 投稿詳細ページの店舗名タップ → 店舗詳細ページ
- ユーザープロフィールの投稿一覧の店舗名タップ → 店舗詳細ページ
- 一覧ページ（タイムライン）の投稿の店舗名タップ → 店舗詳細ページ

## 技術要件

### 1. Backend（Convex）

#### 1-1. 新規クエリ: `shops.getById`
```typescript
// 店舗IDから店舗情報と統計を取得
export const getById = query({
  args: { shopId: v.id("shops") },
  handler: async (ctx, args) => {
    // 店舗情報取得
    // 統計情報計算（投稿数、訪問者数、平均評価、評価分布）
  }
});
```

#### 1-2. 新規クエリ: `noodles.getByShop`
```typescript
// 店舗IDから投稿一覧を取得（フィルター・ソート・ページネーション対応）
export const getByShop = query({
  args: {
    shopId: v.id("shops"),
    sortBy: v.optional(...),
    filterRating: v.optional(...),
    filterGenres: v.optional(...),
    dateFrom: v.optional(...),
    dateTo: v.optional(...),
    limit: v.optional(...),
    offset: v.optional(...)
  },
  handler: async (ctx, args) => {
    // 投稿取得・フィルター・ソート・ページネーション
  }
});
```

#### 1-3. 既存クエリ拡張: `shops.search`
- 投稿数を含める
- 平均評価を含める
- ソートオプションを追加

### 2. Frontend（Next.js + React）

#### 2-1. 新規ページ: `/app/(main)/shops/search/page.tsx`
- 店舗検索画面コンポーネント

#### 2-2. 新規ページ: `/app/(main)/shops/[id]/page.tsx`
- 店舗詳細ページコンポーネント

#### 2-3. 新規コンポーネント: `shop-card.tsx`
- 検索結果の店舗カード表示用

#### 2-4. 新規コンポーネント: `shop-stats.tsx`
- 店舗統計情報表示用

#### 2-5. 既存コンポーネント拡張
- `noodle-card.tsx`: 店舗名をリンク化

### 3. パフォーマンス考慮

- 店舗詳細ページの統計情報はキャッシュ（頻繁に変わらない）
- 投稿一覧は無限スクロールで20件ずつ読み込み
- 画像はLazy Loading対応

### 4. プライバシー考慮

- 鍵アカウントの投稿は非表示（フォロワー以外）
- 既存の`isFollowEnabled`チェックを適用

## UI設計（概要）

### 店舗検索画面
```
┌─────────────────────────────┐
│ 🔍 店舗を探す                │
├─────────────────────────────┤
│ [検索ボックス]               │
│                             │
│ 検索結果:                   │
│ ┌─────────────────────────┐ │
│ │ 🏪 ○○ラーメン           │ │
│ │ 東京都渋谷区...         │ │
│ │ ⭐️ 4.5 | 25件の投稿   │ │
│ └─────────────────────────┘ │
│ ┌─────────────────────────┐ │
│ │ 🏪 △△軒                │ │
│ │ 神奈川県横浜市...       │ │
│ │ ⭐️ 4.2 | 18件の投稿   │ │
│ └─────────────────────────┘ │
└─────────────────────────────┘
```

### 店舗詳細ページ
```
┌─────────────────────────────┐
│ ← 戻る                      │
├─────────────────────────────┤
│ 🏪 ○○ラーメン              │
│ 📍 東京都渋谷区1-2-3        │
│ 🚃 渋谷駅                   │
│                             │
│ 統計:                       │
│ 📊 25件 | 👥 18人 | ⭐️ 4.5 │
│                             │
│ [フィルター] [ソート]        │
├─────────────────────────────┤
│ 投稿一覧:                   │
│ ┌─────────────────────────┐ │
│ │ 👤 ユーザーA 2024/12/10 │ │
│ │ 特製醤油ラーメン ⭐️⭐️⭐️⭐️⭐️│ │
│ │ [📷 写真]               │ │
│ │ 美味しかった！          │ │
│ │ #醤油 #こってり         │ │
│ │ ❤️ 12                   │ │
│ └─────────────────────────┘ │
│ ┌─────────────────────────┐ │
│ │ ...                     │ │
│ └─────────────────────────┘ │
└─────────────────────────────┘
```

## 実装の優先順位

### Phase 1（MVP）
1. ✅ 既存: 店舗検索API（`shops.search`）
2. 🆕 店舗詳細ページ基本表示（店舗情報 + 投稿一覧）
3. 🆕 `noodles.getByShop` クエリ実装
4. 🆕 店舗名リンク化（既存の投稿カードから遷移）

### Phase 2（拡張）
5. 🆕 店舗検索画面（専用ページ）
6. 🆕 統計情報の表示（平均評価、訪問者数等）
7. 🆕 フィルター機能（評価、ジャンル）

### Phase 3（最適化）
8. 🆕 ソート機能の拡張
9. 🆕 期間フィルター
10. 🆕 パフォーマンス最適化

## 注意事項

- 既存のラーメン投稿の表示ロジックを再利用
- 鍵アカウント対応は既存の`isFollowEnabled`チェックを適用
- 店舗情報の編集機能は今回のスコープ外（将来的に検討）
- 店舗の統合・削除機能も今回のスコープ外（管理者機能として別途検討）

## 参考

- 類似機能: ユーザープロフィールページ (`/users/[id]/page.tsx`)
- 投稿一覧表示: `noodle-card.tsx`
- 検索機能: 既存の`shops.search`
